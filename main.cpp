#include <iostream>

using namespace std;

int main()
{
    int x, y;
    cout << "Give me two integer numbers: ";
    cin >> x >> y;
    cout << x << " + " << y << " = " << (x + y) << endl;
    return 0;
}
